import * as debug from "./debug";
import * as dictionary from "./dictionary";
import * as value from "./value";

// TODO: add "branch"? for having conditions in the stage?
export const stock = {
  debug,
  dictionary,
  value,
};
