// in every programming language we have functions and variables but there are also the rest things such as
// loops, conditions and other syntactic sugar made for the convenience - actions are such kind of that.

import * as debug from "./debug";
import * as operation from "./operation";
import * as value from "./value";

export const stock = { debug, operation, value };
