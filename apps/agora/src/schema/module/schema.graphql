enum ModuleType {
  text
  table
}

type Module {
  id: Int!
  createdAt: DateTime!
  type: ModuleType
  config: JSONObject!
}

extend type Mutation {
  updateModule(moduleId: Int!, key: String!, value: JSON!): Module!
  addModule(type: ModuleType!, config: JSONObject!): Module!
}

extend type Query {
  modules: [Module!]!
}
