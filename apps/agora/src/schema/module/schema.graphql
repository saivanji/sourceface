enum ModuleType {
  text
  table
}

type Module {
  id: Int!
  createdAt: DateTime!
  type: ModuleType
  position: Position!
  config: JSONObject!
}

extend type Mutation {
  addModule(
    type: ModuleType!
    config: JSONObject!
    position: Position!
  ): Module!
  updateModule(moduleId: Int!, key: String!, value: JSON!): Module!
}

extend type Query {
  modules: [Module!]!
}
