# enum Database {
#   POSTGRES
# }

# type Source {
#   id: Int!
#   createdAt: DateTime!
#   database: Database!
#   name: String!
#   connection: String!
# }

# type SourceQuery {
#   id: Int!
#   createdAt: DateTime!
#   source: Source!
#   name: String!
#   value: String!
# }

extend type Mutation {
  executeQuery(queryId: Int!, args: JSONObject): JSON
}

extend type Query {
  test: String
}
