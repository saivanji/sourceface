type Module {
  id: UUID!
  positionId: Int!
  createdAt: DateTime!
  name: String!
  type: ModuleType!
  config: JSONObject!
  actions: [Action]!
  layouts: [Layout!]!
}

enum ModuleType {
  button
  container
  input
  table
  text
}

extend type Mutation {
  createModule(
    moduleId: UUID!
    type: ModuleType!
    config: JSONObject!
    position: PositionInput!
  ): Module!
  configureModule(moduleId: UUID!, key: String!, value: JSON!): Module!
  # TODO: make sure all related child layouts are removed as well
  removeModule(moduleId: UUID!): Boolean
}
