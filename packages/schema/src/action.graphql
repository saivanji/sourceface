type Action {
  id: UUID!
  createdAt: DateTime!
  type: ActionType!
  name: String
  config: JSONObject!
  relations: JSONObject!
  pages: [Page!]!
  commands: [Command!]!
}

enum ActionType {
  operation
}

extend type Mutation {
  createAction(
    actionId: UUID!
    moduleId: UUID!
    type: ActionType!
    config: JSONObject!
    relations: JSONObject!
  ): Action!
  updateAction(
    actionId: UUID!
    name: String
    config: JSONObject
    relations: JSONObject
  ): Action!
  removeAction(actionId: UUID!): Boolean!
}
