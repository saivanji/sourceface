type Action {
  id: UUID!
  createdAt: DateTime!
  name: String
  type: ActionType!
  config: JSONObject!
  pages: [Page!]!
  commands: [Command!]!
}

enum ActionType {
  operation
}

extend type Mutation {
  createAction(
    actionId: UUID!
    moduleId: UUID!
    type: ActionType!
    config: JSONObject!
  ): Action!
  updateAction(
    actionId: UUID!
    name: String
    config: JSONObject
    pages: [Int!]
    commands: [Int!]
  ): Action!
  removeAction(actionId: UUID!): Boolean!
}
